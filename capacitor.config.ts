import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.example.app',
  appName: 'cap3-camera-ex',
  webDir: 'dist/cap3-camera-ex',
  bundledWebRuntime: false
};

export default config;
